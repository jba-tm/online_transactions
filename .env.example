DOMAIN=localhost
STACK_NAME=stack_name
SERVER_HOST=http://localhost
SERVER_NAME="Server name"

# BACKEND
BACKEND_CORS_ORIGINS=["http://localhost", "http://localhost:8000", "http://localhost:3000", "http://localhost:8080", "https://localhost", "https://localhost:3000", "https://localhost:8080"]
PROJECT_NAME="Project name"
SECRET_KEY=change_this
FIRST_SUPERUSER=admin@example.com
FIRST_SUPERUSER_PASSWORD=change_this
SMTP_TLS=False
SMTP_PORT=1025
SMTP_HOST=localhost
SMTP_USER=noreply@example.com
SMTP_PASSWORD=change_this
EMAILS_FROM_EMAIL=noreply@example.com
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
FLOWER_PORT=5555
SENTRY_DSN=
INSTALL_DEV=false

# JWT
JWT_SECRET_KEY="change_this"
JWT_PRIVATE_KEY="change_this"
JWT_PUBLIC_KEY="change_this"

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# DATABASE
DATABASE_DRIVER=postgresql
DATABASE_HOST=db
DATABASE_PORT=5432
DATABASE_USER=change_this
DATABASE_PASSWORD=change_this
DATABASE_NAME=change_this

# PgAdmin
PGADMIN_LISTEN_PORT=5050
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=change_this
PGADMIN_HTTPS_PORT=5051

# MAILHOG
MAILHOG_SMTP_SERVER_PORT=1025
MAILHOG_WEB_UI_PORT=8025
MH_HOSTNAME=localhost

# DOZZLE
DOZZLE_PORT=9999
